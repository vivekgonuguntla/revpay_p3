import { CardTypeIconPipe } from './card-type-icon.pipe';

describe('CardTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CardTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
